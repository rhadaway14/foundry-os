[project]
name = "foas"
version = "0.1.0"
description = "Foundry OS Architecture Specification Toolchain"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0.1",
  "networkx>=3.2.1",
  "rich>=13.7.0"
]

[project.scripts]
foas = "foas.cli.main:main"

[tool.setuptools]
packages = ["foas"]
